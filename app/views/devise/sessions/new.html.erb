<div class="jumbotron text-center">
	<h1>Welcome to Chores@4030!</h1>

	<p class="lead">Chores at 4030 is here to help you and your housemates manage your chores! It uses email reminders to keep you on top of your mess.</p>

	<small><em><q>Written in Rails for CIS 196 at UPenn. Powered by SendGrid and Devise. Beautified with Bootstrap.</q></em><small>
</div>

<br></br>

<div class="container-fluid">
	<div class="row-fluid">
		<div class="span4 form-actions">
			<br></br>
			<h2 class="form-signin-heading">Sign In</h2>

			<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
				<div class="form-inputs">
					<%= f.input :email, :required => false, :autofocus => true %>
					<%= f.input :password, :required => false %>
					<label class="inline">
					<%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>
					</label>
				</div>

				<div class="form-actions">
					<%= f.button :submit, 'Sign In', :class => 'btn btn-large btn-success' %>
				</div>
			<% end %>

			<br></br>

			<%= render "devise/shared/links" %>
		</div>

		<div class="span8 img-polaroid">
			<%= image_tag '/assets/images/4030.png', alt: 'the house' %>
		</div>
	</div>
</div>
